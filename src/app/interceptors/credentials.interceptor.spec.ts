import {CredentialsInterceptor } from './credentials.interceptor';

describe('CredentialsInterceptor', () => {
  it('should create an instance', () => {
    expect(new CredentialsInterceptor()).toBeTruthy();
  });
});
